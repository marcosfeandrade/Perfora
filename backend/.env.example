PORT=4000
# Use estas credenciais com o Docker (docker-compose up -d)
# Se o container existente usa porta 5434, use: localhost:5434
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/perfora?schema=public"
CORS_ORIGIN="http://localhost:3000"
JWT_SECRET="sua-chave-secreta-minimo-32-caracteres"
